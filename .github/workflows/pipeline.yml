name: Pipeline
on:
  push:
    branches:
      - main
jobs:
  test:
    name: Transition
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@main
      - name: Print text
        run: echo "Text"
      - name: Print text2
        run: echo "Text2"
